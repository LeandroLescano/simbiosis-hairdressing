---
import NavbarItem from '@components/Navbar/NavbarItem.astro';
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<header class="sticky top-0 z-50 bg-white">
			<nav class="px-10 py-3 border-b-[1px] flex place-content-between items-center">
				<img src="/svgs/logo.svg">
				<div class="flex gap-9">
					<NavbarItem text="Nosotros" class="text-sm font-medium" section='about' />
					<NavbarItem text="Servicios" class="text-sm font-medium" section='services' />
					<NavbarItem text="Contacto" class="text-sm font-medium" section='contact'/>
					<NavbarItem text="Galería" class="text-sm font-medium" section='gallery'/>
				</di>
			</nav>
		</header>
		<slot />
	</body>
</html>

<style is:global>
	:root {
		--accent: 136, 58, 234;
		--accent-light: 224, 204, 250;
		--accent-dark: 49, 10, 101;
		--accent-gradient: linear-gradient(
			45deg,
			rgb(var(--accent)),
			rgb(var(--accent-light)) 30%,
			white 60%
		);
		scroll-behavior: smooth;
		scroll-margin-top: 5rem;
	}
	html {
		font-family: "SpaceGrotesk", system-ui, sans-serif;
	}
	code {
		font-family:
			Menlo,
			Monaco,
			Lucida Console,
			Liberation Mono,
			DejaVu Sans Mono,
			Bitstream Vera Sans Mono,
			Courier New,
			monospace;
	}
	@font-face {
		font-family: Square;
		src: url('/fonts/Square.ttf');
	}
	@font-face {
		font-family: SpaceGrotesk;
		src: url('/fonts/SpaceGrotesk.ttf');
	}
</style>
